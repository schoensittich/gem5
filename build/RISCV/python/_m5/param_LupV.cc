/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:273
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/LupV.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/riscv/lupv.hh"

#include "dev/lupio/lupio_pic.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_LupV");
    py::class_<LupVParams, PlatformParams, std::unique_ptr<LupVParams, py::nodelete>>(m, "LupVParams")
        .def(py::init<>())
        .def("create", &LupVParams::create)
        .def_readwrite("pic", &LupVParams::pic)
        .def_readwrite("uart_int_id", &LupVParams::uart_int_id)
        ;

    py::class_<gem5::LupV, gem5::Platform, std::unique_ptr<gem5::LupV, py::nodelete>>(m, "gem5_COLONS_LupV")
        ;

}

static EmbeddedPyBind embed_obj("LupV", module_init, "Platform");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyLupVParamsClass
{
  public:
    gem5::LupV *create() const;
};

template <class CxxClass, class Enable=void>
class DummyLupVShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyLupVShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const LupVParams &>>>
{
  public:
    using Params = LupVParams;
    static gem5::LupV *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyLupVShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const LupVParams &>>>
{
  public:
    using Params = DummyLupVParamsClass;
    static gem5::LupV *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::LupV *
DummyLupVShunt<gem5::LupV>::Params::create() const
{
    return DummyLupVShunt<gem5::LupV>::create(*this);
}

} // namespace gem5
